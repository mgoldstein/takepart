<?php

/*
 * Implements hook_ctools_plugin_api()
 * Tells ctools where to find our metatag plugin.
 */
function tp_dtm_ctools_plugin_api($owner, $api){
	if ($owner == 'metatag' && $api == 'metatag') {
		return array('version' => 1);
	}
}