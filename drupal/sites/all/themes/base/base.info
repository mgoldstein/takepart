; Drupal's .info files allow themers to easily specify some of the static
; properties of your theme. Properties such as its name, stylesheets,
; javascripts, and block regions.
;
; Drupal 7 stores a cache of the data in this .info files. If you modify any
; lines in this file, you MUST refresh Drupal 7's cache by simply visiting the
; Appearance page at admin/appearance.


screenshot  = ace-of-base.jpg
name        = Base
description = The base theme for TakePart themes

; This theme is compatible with Drupal 7 core. And it is a sub-theme of Zen.

core        = 7.x
base theme  = zen

; add some JavaScripts to the theme.
scripts[] = js/tpanalytics.js
scripts[] = js/jquery.tpinfographic.js
scripts[] = js/tpmodal.jquery.js

; For Picture 2.x
; While this theme is disabled, the below breakpoints essentially dont do anything
; However, they are used with tp_theme_support update scripts to make use of
breakpoints[base_large_desktop] = (min-width: 980px)
breakpoints[base_large_tablet] = (min-width: 768px)
breakpoints[base_large_mobile] = (min-width: 0px)