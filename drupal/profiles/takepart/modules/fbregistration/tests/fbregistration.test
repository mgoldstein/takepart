<?php
class FBRegistrationTestCase extends DrupalUnitTestCase {

  public static function getInfo() {
    // Note: getInfo() strings should not be translated.
    return array(
        'name' => 'Facebook registration unit tests',
        'description' => 'Test that the base reg functionality works properly.',
        'group' => 'Facebook',
    );
  
  }
  
  function testEmptyMySQLDate() {
  
    $result = takepart_facebookapis_test(NULL);
    // Note: Test output messages should not be translated.
    $message = 'A NULL value should return TRUE.';
    $this->assertTrue($result, $message);
  
  }
  
}
